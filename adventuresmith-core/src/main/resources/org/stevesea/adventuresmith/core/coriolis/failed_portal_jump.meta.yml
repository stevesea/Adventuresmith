---
name: Failed Portal Jump
input:
  displayTemplate: "Blind jump: {{isblind}}"
  params:
  - name: isblind
    uiName: Is Blind?
    helpText: Was it a blind jump?
    defaultValue: no
    values:
    - yes
    - no

